# Use of a local Dagger package

Then you have to vendor Dagger dependencies ruunning:

```
dagger project update
```

You can now run the task to transform a file into FIGlet running:

```
dagger do build
```